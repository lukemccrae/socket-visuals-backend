scalar AWSJSON
directive @aws_subscribe(mutations: [String!]!) on FIELD_DEFINITION

type NoteEvent {
  note: Int!
  velocity: Float!
}

type Mutation {
  publishNote(note: Int!, velocity: Float!): NoteEvent
}

type Subscription {
  subscribe2Notes: NoteEvent @aws_subscribe(mutations: ["publishNote"])
}

type Query {
  subscriberCount: Int!
}

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}
